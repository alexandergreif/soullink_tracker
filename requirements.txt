# SoulLink Tracker Portable Edition - Requirements
# Core dependencies for the portable version

# FastAPI and server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Database
sqlalchemy>=2.0.0
alembic>=1.12.0
aiosqlite>=0.19.0

# API and validation
pydantic>=2.5.0
websockets>=12.0

# Authentication and security
cryptography>=41.0.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
PyJWT>=2.8.0

# Optional dependencies for enhanced features
# System tray (optional - fallback if not available)
pillow>=10.0.0
pystray>=0.19.0

# Production watcher HTTP client
requests>=2.31.0

# Build dependencies (for development)
pyinstaller>=6.0.0

# Testing dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-playwright>=0.4.0
hypothesis>=6.88.0
httpx>=0.25.0  # For async test client